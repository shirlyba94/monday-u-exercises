const getItemsView = state => state.itemsView;
//const getShowDone = (state) => getItemsView(state).showDone;
//const getSearchInput = (state) => getItemsView(state).searchInput;
//const getListFilterFilter = (state) => getItemsView(state).listFilter;